import {getBusinessContext} from '@/lib/business-context'; import {createClient} from '@/lib/supabase/server'; import MenuManagerClient from '@/components/menu-manager-client'; import Link from 'next/link';
export default async function MenuPage(){const {business}=await getBusinessContext();const supabase=await createClient();const {data:menus}=await supabase.from('menus').select('*').eq('business_id',business.id).order('created_at',{ascending:false});return <div className="min-h-screen bg-slate-50"><header className="flex items-center justify-between border-b bg-white px-5 py-4"><div><h1 className="font-bold text-emerald-600">{business.name}</h1><p className="text-xs text-slate-500">Manajemen Menu</p></div><nav className="flex gap-2 text-sm"><Link className="rounded-lg px-3 py-2 hover:bg-slate-100" href="/dashboard">Kasir</Link><Link className="rounded-lg bg-emerald-50 px-3 py-2 text-emerald-700" href="/dashboard/menu">Menu</Link></nav></header><main className="mx-auto max-w-6xl p-5"><MenuManagerClient initialMenus={menus||[]} businessId={business.id}/></main></div>}
