'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {createClient} from '@/lib/supabase/client';
export default function OnboardingPage(){const [name,setName]=useState('');const [phone,setPhone]=useState('');const [address,setAddress]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();const supabase=createClient();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const {data:{user}}=await supabase.auth.getUser();if(!user){router.push('/login');return}const {data:b,error:be}=await supabase.from('businesses').insert({name:name.trim(),phone:phone.trim()||null,address:address.trim()||null,tax:0,service:0}).select().single();if(be||!b){setError(be?.message||'Gagal membuat bisnis.');setLoading(false);return}const {error:me}=await supabase.from('business_members').insert({business_id:b.id,user_id:user.id,role:'owner'});if(me){setError(me.message);setLoading(false);return}router.push('/dashboard');router.refresh()}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-2xl border bg-white p-7 shadow-sm"><h1 className="text-xl font-bold text-emerald-600">Buat Bisnis</h1>{error&&<div className="rounded-lg bg-red-50 p-3 text-sm text-red-600">{error}</div>}<input required className="w-full rounded-lg border p-3" placeholder="Nama Bisnis" value={name} onChange={e=>setName(e.target.value)}/><input className="w-full rounded-lg border p-3" placeholder="Nomor Telepon" value={phone} onChange={e=>setPhone(e.target.value)}/><textarea className="w-full rounded-lg border p-3" placeholder="Alamat" value={address} onChange={e=>setAddress(e.target.value)}/><button disabled={loading} className="w-full rounded-lg bg-emerald-600 py-3 font-semibold text-white">{loading?'Menyimpan...':'Buat Bisnis'}</button></form></main>}
